# Decision Engine

Prototype rule engine for deciding when Amica should trigger an SOS workflow.

Supported trigger types:

- `timer_expired_no_response`
- `secret_phrase_detected`
- `manual_sos_pressed`
