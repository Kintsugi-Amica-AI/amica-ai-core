# AI Architecture

Amica AI Core is split into three prototype domains:

- Plate OCR for extracting vehicle number plates.
- Voice SOS for detecting a configured secret phrase.
- Decision engine for mapping safety triggers to SOS actions.

Production implementation should define model hosting, privacy boundaries, audit logging, and mobile/backend contracts before real user data is processed.
